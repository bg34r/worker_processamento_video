apiVersion: v1
kind: Service
metadata:
  name: video-worker-service
  namespace: video-processing
  labels:
    app: video-worker
spec:
  selector:
    app: video-worker
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP  # Interno ao cluster